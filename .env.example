# Supabase public frontend values
# These values are safe to use in browser code only when row level security is configured.
SUPABASE_URL=https://ifyyflvxqkazndkwffvm.supabase.co
SUPABASE_ANON_KEY=replace-with-supabase-anon-key

# Server-only values. Do not expose these in frontend code.
SUPABASE_SERVICE_ROLE_KEY=replace-only-on-server

# Email sender for notification workflows.
NOTIFICATION_FROM_EMAIL=svbdruzstevna386@gmail.com

# App URL for local testing and deployed environments.
APP_URL=http://127.0.0.1:4173
